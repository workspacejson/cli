node_modules/
dist/
packages/*/dist/
packages/*/node_modules/
packages/*/.tmp-*/
.DS_Store
coverage/
*.log

# Local planning/config — not for public repo
.planning/
.claude/
CLAUDE.md

# Automated audit outputs — not for public repo
audit-output/

# Local swarm system — not for public repo
.vreko-swarm/

# Local artifacts
agents-workspace-cannon-repo/
audit-findings/
shared-baseline-audit-2026-05-10.md

# stale parity cache from before it moved to ~/.cache/workspacejson/cli-parity;
# still ignored so a leftover clone never gets committed
.parity-cache/
