# Baseline validity

PASS. Across all 200 frozen queries, 143 (71.5%) have at least one depth-4 test→source path; B2 differs from B1 on 200/200 rankings and moves 1,874 top-10 memberships in total. The initial frozen-query import-edge ablation (remove resolved edges) changed the ranking and was caught, so the dependency surface is non-inert.

This establishes a distinct static information surface, not a dependency or required-edit relation.
