# META-379 report

`HISTORY_RETAINS_RESIDUAL_SIGNAL`: the stronger deterministic T0 TypeScript graph improves substantially over B1 but does not close the frozen historical margin at recall@10 (H−B2 0.06348).

This does not establish that history is useful generally, that co-updated tests are dependencies or required edits, or that any test would catch a regression. The only earned next step is a separately preregistered unseen-repository TypeScript/JavaScript replication; do not create it automatically.
